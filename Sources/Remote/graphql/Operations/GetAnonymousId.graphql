mutation GetAnonymousId($anonymousId: UUID, $sourceId: ID!, $hiddenFields: [hiddenFieldInput], $preview: Boolean) {
  getAuthor(anonymousID: $anonymousId, sourceID: $sourceId, hiddenFields: $hiddenFields, preview: $preview) {
    anonymousID
  }
}
